import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CadFormulaPage } from './cad-formula';

@NgModule({
  declarations: [
    CadFormulaPage,
  ],
  imports: [
    IonicPageModule.forChild(CadFormulaPage),
  ],
})
export class CadFormulaPageModule {}
